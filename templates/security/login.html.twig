{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <div class="block-log">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        {% if app.user %}
            <div class="mb-3">
                You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
            </div>
        {% endif %}
        <div class="block-log-header">
            <h6 class="text-uppercase mb-0">Sign in</h6>
        </div>
        <div class="block-log-body">
            <p class="lead">Already our customer ?</p>
            <p class="text-muted">Phasellus eget vestibulum magna, eu rutrum nibh. Sed eget cursus tortor. Mauris auctor pretium tempus. Integer tempor malesuada neque eget fermentum.</p>
            <hr>

            <form method="post">
                <label for="inputEmail">Email</label>
                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>
                <label for="inputPassword">Password</label>
                <input type="password" name="password" id="inputPassword" class="form-control" required>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                {#
                    Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                    See https://symfony.com/doc/current/security/remember_me.html

                    <div class="checkbox mb-3">
                        <label>
                            <input type="checkbox" name="_remember_me"> Remember me
                        </label>
                    </div>
                #}
                <div class="form-group text-center">
                    <button class="btn btn-outline-dark mt-3" type="submit">
                        Sign in
                    </button>
                </div>
            </form>
        </div>  
    </div>
{% endblock %}
